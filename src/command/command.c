/*EXPORT

enum CommandType {
  NONE,
  INSERT,
  DELETE,
  ENTER,
  UP,
  DOWN,
  LEFT,
  RIGHT,
  // SELECT_UP,
  // SELECT_DOWN,
  SELECT_LEFT,
  SELECT_RIGHT,
  SAVE_OVERRIDE,
  EXIT
};

typedef struct _command {
  enum CommandType command_key;
  utf8char command_value;
} command;

*/
